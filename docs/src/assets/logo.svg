<!-- Placeholder logo for MultiHubForecaster.jl, seeded once by `scaffold` and never
     touched again by `scaffold_update` (package-owned, like LICENSE). Replace this
     file with a real logo; the README title and docs build already point at
     this exact path (see docs/make.jl's README -> index.md step), so no
     other wiring changes are needed when it is swapped out. -->
<svg xmlns="http://www.w3.org/2000/svg" width="150" height="150"
     viewBox="0 0 150 150" role="img"
     aria-label="MultiHubForecaster logo placeholder">
  <title>MultiHubForecaster logo placeholder</title>
  <rect width="150" height="150" rx="18" fill="#389826"/>
  <text x="75" y="95" font-family="sans-serif" font-size="72"
        font-weight="700" fill="#ffffff" text-anchor="middle">
    M
  </text>
</svg>
